# Plan — Nors v1

Design: `docs/specs/2026-09-09-nors.md`  
Gate: design locked 2026-09-09 (Glance Apps live, mark chosen).

## Out of scope (v1)

- In-app LLM generate
- Glance Nors widget (bookmark only when `/nors/` ships)
- npm publish / Skadi-style installer package (install.sh is enough)
- `@thisbeyond/solid-dnd` (native HTML5 only)
- Cloudflare Access rules (Burak pastes; document only)

---

### 1. Scaffold repo + PocketBase migration — DONE
- **Files:** `package.json`, `web/` (Solid+Vite), `pb_migrations/1789000000_nors_notes.js`, `pb_public/nors/`
- **Verify:** `bun run build` → `pb_public/nors/` (~32 KB; JS gzip ~7.6 KB)
- **Done when:** build emits under `/nors/` asset paths

### 2. Auth shell + empty dashboard — DONE
- **Files:** `web/src/App.tsx`, `web/src/lib/pb.ts`, `web/src/App.css`
- **Do:** superuser login → localStorage token; list notes; empty state; mark favicon
- **Verify:** production build paths `/nors/assets/…`; no PocketBase JS SDK (fetch only)
- **Done when:** login + empty grid shell ships in the build

### 3. Reader + Editor (markdown)
- **Files:** `web/src/pages/*`, small markdown lib (pick lightest that supports fences)
- **Do:** hash routes `#/` dashboard, `#/n/<slug>` reader, `#/edit/<slug|$new>` editor; Mermaid loaded only if fence present
- **Verify:** build size note in README (`du -sh pb_public/nors`); manual smoke with a fixture note JSON
- **Done when:** create draft → save → publish → read round-trip works against a local or server PB

### 4. Native DnD reorder
- **Files:** dashboard card list
- **Do:** HTML5 drag-and-drop updates `sort` via PB API; pinned stay first
- **Verify:** reorder two cards, refresh, order persists
- **Done when:** no DnD library in `package.json`

### 5. Seeds + install path
- **Files:** `seeds/*.md`, `scripts/import-seeds.ts` or `.sh`, `deploy/install.sh`, Access/Caddy notes in `deploy/`
- **Do:** three diagram seeds (cli-shelf, penolox-server, vault-engine); copy migrations + `pb_public/nors` to `/opt/pocketbase`; document Access destinations
- **Verify:** dry-run import idempotent; install script is paste-ready (no agent SSH write)
- **Done when:** Burak can deploy with documented paste blocks

### 6. Glance Apps link for Nors
- **Files:** `deploy/patch-glance-apps.py` or a follow-up patch
- **Do:** add Nors bookmark next to Skadi once `/nors/` is live
- **Verify:** Apps page shows both links
- **Done when:** icon `/assets/nors.png` (rasterized mark) on server
